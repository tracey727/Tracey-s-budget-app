# workflow-engine

Generic work-item engine: single current owner, due dates, transfer-with-acceptance, Green/Amber/Red/Recovery state logic, escalation, close/reopen with reason. Built in Phase 7 (Core Work Ownership Engine) and reused by every module in Phase 8+ rather than re-implemented per module — see `docs/product/LOSS_MAP_BASELINE.md` §7.
