# audit

Append-only audit event emission/query helpers, used by every module that writes material business data. Built starting Phase 6 (Authentication, Authorisation & Audit) alongside the auth layer, per `docs/security/SECURITY_PRIVACY_GOVERNANCE.md` "Audit requirements".
